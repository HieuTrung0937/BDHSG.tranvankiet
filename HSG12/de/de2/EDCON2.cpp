#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define TASK "EDAYCON2"

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    freopen(TASK".INP", "r", stdin);
    // freopen(TASK".OUT", "w", stdout);
    int n, mod; cin>>n>>mod;
    vector<int> L(n + 1), R(n + 1);
    vector<int> a(n + 1);
    for(int i = 1; i <= n; i++)
    {
        L[i] = a[i]
        for(int j =  i; j <= )
    }
    return 0;
}