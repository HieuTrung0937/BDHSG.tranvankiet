#include <bits/stdc++.h>

using namespace std;

#define TASK "BAI2_1"
bool chekNguyenTo(x)
{
    if(x<2) return false;
    if(x == 2) return true;
    if(x == 3) return true;
    int n = sqrt(x)
    for(int i = 3; i < n; i++)
    {
        
    }
}
int Tong_Uoc_Nguyen_To(n)
{
    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen(TASK".INP", "r" ,stdin);
    freopen(TASK".OUT","w",stdout);
    int n = 0; cin >> n;
    cout<<n;
    return 0;
}